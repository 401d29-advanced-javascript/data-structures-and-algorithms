'use strict';

const array = [1,2,3,4,5,6,7];
const element = 5;
const insertShiftArray = (array, element) => {
  for(i = 0; i < array.length - 1; i++) {
    if(array[index] > array.length / 2) {
      let middleOfArray = array[index];
      console.log('Middle of Array:', middleOfArray);
      let startArr = [...array, ...middleElement]
      console.log('start: ', startArr);
      break;
    };
  };
};
//INCOMPLETE
console.log('Array Call: ', insertShiftArray(array,element));

module.exports = (insertShuftArray);